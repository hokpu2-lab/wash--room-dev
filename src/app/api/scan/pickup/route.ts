import {pickupLaundryOrder} from "@/lib/laundry-order/pickup";
const headers={"Cache-Control":"private, no-store, max-age=0, must-revalidate","CDN-Cache-Control":"no-store","Referrer-Policy":"no-referrer","X-Content-Type-Options":"nosniff","X-Robots-Tag":"noindex, nofollow, noarchive"} as const;
export async function POST(request:Request){let body:unknown;try{if(!request.headers.get("content-type")?.toLowerCase().includes("application/json"))return Response.json({kind:"invalid",reasonCode:"invalid_request"},{status:400,headers});body=await request.json()}catch{return Response.json({kind:"invalid",reasonCode:"invalid_request"},{status:400,headers})}const value=typeof body==="object"&&body!==null?body:{};const result=await pickupLaundryOrder({qrToken:"qr_token"in value?value.qr_token:null,requestId:"change_request_id"in value?value.change_request_id:null});return Response.json(result,{status:result.kind==="failed"?503:result.kind==="invalid"?400:200,headers})}
