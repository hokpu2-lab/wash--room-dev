---
status: accepted
---

# 匿名洗衣車掃碼與自建帳號密碼驗證並存

一般送洗人員仍以固定洗衣車 QR 匿名執行送單與取件；需要進入內部系統的人員改用自建短帳號與 Supabase Auth 密碼驗證。短帳號在伺服器端映射為不可寄信的內部 Auth Email，通知 Email 另行保存；系統關閉公開註冊、只接受預先核准 membership，初始一次性強密碼必須在首次登入後更換。`admin` 只是首位洗衣主管的登入帳號，不是繞過 RLS 的超級角色；此方案保留 Supabase JWT 與既有 RLS 邊界，同時避免把聯絡 Email 當成日常登入識別。
